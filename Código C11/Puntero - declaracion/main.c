//Puntero - Declaración de una variable puntero a entero, y de una variable de tipo entero

#include <stdio.h>

int main(void) {

	//Declaración de una variable de tipo puntero que apuntaría a la dirección de memoria de una variable de tipo entero
	int* numPtr;  

	//Declaración de una variable de tipo entero
	int num;
}